# Schistosoma myeloid multiomics

This repository contains the downstream analyses used to reproduce the ATAC-seq and RNA-seq results presented in the manuscript.

Raw sequencing data are available through GEO (accession GSE337316). Processed data required to reproduce the analyses are included in this repository or are available through Zenodo where noted.

## ATAC-seq preprocessing

Raw ATAC-seq FASTQ files were processed using the `nf-core/atacseq` pipeline executed with Nextflow and Apptainer.

The preprocessing included:

- mm10 canonical reference genome
- GENCODE vM25 gene annotation
- ENCODE mm10 blacklist

TOBIAS (v0.17.0) footprinting was performed on merged replicate BAM files using the `ATACorrect`, `FootprintScores`, and `BINDetect` modules with the JASPAR 2024 CORE vertebrate non-redundant motif collection.

The processed outputs generated by these workflows serve as inputs for the downstream analyses contained in this repository.

## Repository structure

- `config.R` — global paths used by all scripts
- `data/` — processed input datasets
- `results/` — intermediate analysis results
- `figures/` — manuscript figures
- `scripts/` — analysis scripts
- `metadata/` — sample metadata
- `docs/` — additional documentation

## ATAC-seq

Processed ATAC-seq inputs are located in:

- `data/atacseq/bigwig/`
- `data/atacseq/combined/`
- `data/atacseq/peaks/`
- `data/atacseq/processed/`
- `data/atacseq/tobias/`

Analysis scripts are located in:

- `scripts/atacseq/`

Outputs are written to:

- `results/atacseq/`
- `figures/atacseq/`

## RNA-seq

Analysis scripts are located in:

- `scripts/rnaseq/`

Outputs are written to:

- `results/rnaseq/`

## Reproducibility

All active analysis scripts use repository-relative paths defined in `config.R`.
